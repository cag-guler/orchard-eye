# Outputs

This directory is reserved for generated outputs produced by the computer vision pipeline.

Example outputs include:

- detection visualizations
- merged treatment zones
- disease heatmaps
- calculated metrics

Example visual outputs used in the README are stored in:

assets/images
